define(
    [
        'angular',
        '../base/base-module'
    ],
    function (angular) {
        config.$inject = ['$stateProvider', '$urlRouterProvider'];
        function config($stateProvider, $urlRouterProvider) {

        }
        return config;
    });